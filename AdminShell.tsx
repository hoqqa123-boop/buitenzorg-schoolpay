import Link from "next/link";import {createClient} from "@/lib/supabase/server";import {logout} from "@/app/admin/actions";const nav=[["Dashboard","/admin/dashboard"],["Siswa","/admin/students"],["Tagihan","/admin/invoices"],["Pembayaran","/admin/payments"],["Laporan","/admin/reports"]] as const;export async function AdminShell({title,children}:{title:string;children:React.ReactNode}){const s=await createClient();const {data:{user}}=await s.auth.getUser();const {data:p}=user?await s.from("profiles").select("full_name,role").eq("id",user.id).single():{data:null};return <div className="app-shell"><aside className="sidebar"><div className="sidebar-brand"><div className="brand-mark small">BIS</div><div><strong>Buitenzorg</strong><span>International School</span></div></div><nav>{nav.map(([label,href])=><Link key={href} href={href}>{label}</Link>)}</nav><form action={logout}><button className="logout-btn">Keluar</button></form><div className="sidebar-foot">School Finance</div></aside><main className="content"><header className="topbar"><div><p className="eyebrow dark">Finance Administration</p><h1>{title}</h1></div><div className="user-pill"><span>{(p?.full_name||"U").slice(0,2).toUpperCase()}</span><div><strong>{p?.full_name||"User"}</strong><small>{p?.role||""}</small></div></div></header>{children}</main></div>}
